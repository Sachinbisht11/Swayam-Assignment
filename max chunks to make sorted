class Solution {
    public int maxChunksToSorted(int[] arr) {
        int count=0;
        int mcount=0;
        for(int i=0;i<arr.length;i++){
            mcount=Math.max(mcount,arr[i]);
            if(mcount==i){
                count++;
            }
        }
        return count;
    }
}
